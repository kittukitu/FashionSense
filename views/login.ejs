<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login - Fashion Sense</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="shortcut icon" href="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcTAQHwjXyAiIxUCnh3-cFywq0K4ZmDFo20YAw&s">
  <style>
    body {
      background-image: url('https://i.ibb.co/99W36WBJ/pexels-kseniachernaya-3965545.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
    }
    .login-box {
      background-color: rgba(255, 255, 255, 0.9); /* Light, semi-transparent background */
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
    }
    @keyframes gradient-x {
    0% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
    100% {
      background-position: 0% 50%;
    }
  }

  .animate-gradient-x {
    background-size: 200% 200%;
    animation: gradient-x 3s ease infinite;
  }
  </style>
</head>
<body class="flex items-center justify-center h-screen bg-gray-900 bg-opacity-60">
  <!-- Login Form -->
  <div class="w-full max-w-md p-10 rounded-lg login-box">
    <h1 class="mb-4 text-4xl font-extrabold text-center text-transparent bg-clip-text bg-gradient-to-r from-green-400 via-yellow-600 to-brown-600 animate-gradient-x">
      FashionSense
    </h1>
    <p class="mb-6 text-lg text-center text-gray-600">Your personal AI stylist is just a login away!</p>

    <!-- Display error message if any -->
    <% if (error) { %>
      <div class="p-4 mb-4 text-white bg-red-500 rounded">
        <p><%= error %></p>
      </div>
    <% } %>

    <!-- Login Form -->
    <form action="/login" method="POST" class="space-y-6">
      <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
      
      <div>
        <label for="email" class="block mb-2 text-sm font-medium text-gray-700">Email</label>
        <input type="email" name="email" id="email" required
               class="w-full p-4 text-gray-800 placeholder-gray-400 transition-shadow border border-gray-300 rounded-md bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>

      <div>
        <label for="password" class="block mb-2 text-sm font-medium text-gray-700">Password</label>
        <input type="password" name="password" id="password" required
               class="w-full p-4 text-gray-800 placeholder-gray-400 transition-shadow border border-gray-300 rounded-md bg-gray-50 focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>

      <button type="submit" class="w-full py-3 text-white transition-colors duration-300 ease-in-out rounded-md bg-emerald-600 hover:bg-green-700">Login</button>
    </form>

    <p class="mt-4 text-sm text-center text-gray-600">
      <a href="/register" class="text-emerald-500 hover:text-emerald-400">Don't have an account? Register here.</a>
    </p>
  </div>
</body>
</html>